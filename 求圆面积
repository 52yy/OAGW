#include <stdio.h>
#define PI 3.1415926
int main()
{
    double radius,area;
    printf("请输入圆的半径：");
    scanf("%lf",&radius);
    area=PI*radius*radius;
    printf("圆的面积为：%.2lf\n",area);
    return 0;
}
