#include <stdio.h>
int main()
{
    int sz[6][6],i,n,j,k=0;
    printf("Input n:");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        for(j=0;j<n;j++)
        {
            sz[i][j]=(i+1)*(j+1);
            printf("%4d",sz[i][j]);
        }
        printf("\n");
    }
    return 0;
}
