#include <stdio.h>
int main()
{
    int sz[10],x,i,j,k,l;
    j=0;
    for(i=0;i<10;i++)
        scanf("%d",&sz[i]);
    scanf("%d",&x);
    for(i=0;i<10;i++)
    {
        if(sz[i]==x)
        {
            j++;
            if(j==1)
                printf("Index is:")；
            printf("%3d",i);
        }
    }
    if(j==0)
        printf("Not Found");        
    return 0;
}
