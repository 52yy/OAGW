#include <stdio.h>
int main()
{
    char c;
    while((c=getchar())!='\n')
    {
        if(c>='a'&&c<='z'||c>='A'&&c<='Z')
            printf("%c",c);
        else
        {
            continue;
        }
    }
    return 0;
}
