#include <stdio.h>
int main()
{
    int sz[4][4],i,j,sum=0;
    for(i=0;i<4;i++)
    {
        for(j=0;j<4;j++)
        {
            scanf("%d",&sz[i][j]);
            if(i==j||i+j==3)
                sum+=sz[i][j];
        }
    }
    printf("sum=%d",sum);
    return 0;
}
