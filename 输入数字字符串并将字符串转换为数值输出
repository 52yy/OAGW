#include <stdio.h>
int main()
{
    int i,j,k=0;
    char sz[81];
    printf("Enter a string:");
    gets(sz);
    for(i=0;sz[i];i++)
    {
        k=(sz[i]-'0')+k*10;
    }
    printf("digit=%d",k);
    return 0;
}
