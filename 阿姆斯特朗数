#include <stdio.h>
int main()
{
    int a,s,d,f;
    for(a=100;a<1000;a++)
    {
        s=a%10;
        d=(a/10)%10;
        f=(a/100)%10;
        if(a==s*s*s+d*d*d+f*f*f)
        {
            printf("%6d",a);
        }
    }
    return 0;
}
