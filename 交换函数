#include <stdio.h>
int swap(int x,int y)
{
    int n;
    n=x;
    x=y;
    y=n;
    printf("in swap:x=%d,y=%d\n",x,y);
    return 0;
}
int main()
{
    int a=2,b=3;
    printf("before swap:a=2,b=3\n");
    swap(a,b);
    printf("after swap:a=2,b=3\n");
    return 0;
}
