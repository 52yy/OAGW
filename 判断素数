#include <stdio.h>
int main()
{
    int i,a,b=1;
    scanf("%d",&a);
    for(i=2;i<a;i++)
    {
        if(a==2)
            break;
        else if(a%i==0)
        {
            b++;
        }
    }
    if(b==1)
        printf("\nYES\n");
    else
        printf("\nNO\n");
    return 0;
}
