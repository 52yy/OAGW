#include <stdio.h>
int fn(int a,int n)
{
    int i,sum=0,j=0;
    for(i=0;i<n;i++)
    {
        sum=sum*10+a;
        j=j+sum;
    }
    return j;
}
int main()
{
    int a,n,sum;
    printf("Input a&n:");
    scanf("%d",&a);
    scanf("%d",&n);
    printf("sum=%d",sum=fn(a,n));
    return 0;
}
